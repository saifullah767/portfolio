{
  "name": "portfolio-design",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:build": "bash scripts/cloudflare-build.sh",
    "deploy:pages": "npm run pages:build && wrangler pages deploy public --project-name saif-portfolio"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "wrangler": "^4.85.0"
  }
}
